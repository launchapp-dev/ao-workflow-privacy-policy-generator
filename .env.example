# Privacy Policy Generator — Environment Variables
# No external API keys required — uses only local tools (grep, find, python3, diff, jq)
# and Claude models via AO daemon.

# Optional: Override the product directory to scan
# PRODUCT_DIR=./sample-product

# No other keys needed — all processing is done by Claude agents via AO
